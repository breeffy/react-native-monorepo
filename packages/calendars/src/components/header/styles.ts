import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headerText: {
    textAlign: 'center',
    textAlignVertical: 'center',
    marginTop: 8,
    marginBottom: 8
  }
});
